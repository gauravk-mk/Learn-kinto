import KintoClientBase, { SUPPORTED_PROTOCOL_VERSION } from "./base";
import Collection from "./collection";
export default class KintoClient extends KintoClientBase {
    constructor(remote, options = {}) {
        const events = options.events;
        super(remote, Object.assign({ events }, options));
    }
}
export { Collection, SUPPORTED_PROTOCOL_VERSION };
